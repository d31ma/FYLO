/**
 * TTID is consumed as a standalone binary via the vendored shim in
 * `src/vendor/`, so there is no package to import ambient types from. Mirror the
 * shapes FYLO relies on here.
 *
 * @typedef {string} TTID
 * @typedef {{ createdAt: number, updatedAt?: number, deletedAt?: number }} TTIDTimestamps
 */

export {}
